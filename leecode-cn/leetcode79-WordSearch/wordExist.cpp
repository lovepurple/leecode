#include <iostream>
#include <vector>
#include <string>

using namespace std;

class Solution
{
public:
	bool exist(vector<vector<char>>& board, string word);

	bool dfs(vector<vector<char>>& board, int rowIndex, int colIndex, char s);
};

int main()
{
	Solution solution;

	return 0;
}

bool Solution::exist(vector<vector<char>>& board, string word)
{
	for(int i = 0)


}

bool Solution::dfs(vector<vector<char>>& board, int rowIndex, int colIndex, char s)
{
	return false;
}
